请不要再询问 true_posterior 和 repo_compat 的二选一。论文目标已经固定：

    sector = x_error
    ensemble = true_posterior

请先阅读论文 LaTeX 中 Statistical Mechanical Mapping、Order Parameter
和 state preparation process，然后完成以下任务。

一、建立物理契约
1. 新建 data/expander_code/exp101/PHYSICS_CONTRACT.md。
2. 新建同目录 AGENTS.md，要求 exp101 的所有工作以该契约为准，
   根目录 legacy 3D delta-only 公式不适用于论文生产模式。
3. 将我给出的变量表、posterior、coordinate transformation、
   observable 和生产限制写入契约。

二、修正文档中的错误
1. 修正 model.py、notes/01_model_spec.md、plan.md、status.md、report.md：
       x_error/H_Z/X errors 对应 |+>^n -> |+>_L preparation；
       z_error/H_X/Z errors 对应 |0>^n -> |0>_L preparation。
2. 不要交换 assemble_sector_model 当前的 H_X/H_Z 接线，它已经正确。
3. report.md 作为历史报告时，在顶部增加明确 erratum，不要静默改写历史结果。

三、消除变量歧义
1. 将 eta 的新物理名称定义为 epsilon_data_true。
2. 将 delta 定义为 delta_meas。
3. 将 observed_syndrome 改名或别名为 y_eff/effective_syndrome。
4. manifest 中记录：
       state_prep_protocol = plus_Zcheck_X
       syndrome_semantics = effective_y
       physics_model = true_posterior
       physics_contract_version
5. 保留旧字段只作为向后兼容 alias。

四、重命名 legacy 模式
1. 将 repo_compat 的正式名称改为 legacy_delta_only。
2. 可保留 repo_compat deprecated alias。
3. 所有输出、CLI help 和 manifest 明确说明该模式不是论文模型，
   只用于旧 3D 程序回归。
4. 不得把它称为 paper shifted coordinates。
5. 若需要实现 paper shifted coordinates，其能量必须严格是：
       Kp |x XOR epsilon_true| + Kq |H x XOR delta_meas|，
   并与 true_posterior 做逐构型等价测试。

五、修复生产入口
1. run_scan 不得继续默认 ti 后在 k>10 静默使用 pairwise q_top。
2. k>10 请求 TI q_top 时必须 ValueError。
3. pairwise-TI 只允许输出 per-u free-energy gap，字段名中不得出现 q_top。
4. 将已有 pt.py 接入 run_scan，增加 engine=pt 或 engine=auto。
5. engine=auto 至少遵循：
       k<=10 -> full-TI
       k>10,q>0 -> PT
       k>10,q=0 -> validated q=0 path
6. convergence gate 失败的 chunk 标记 INVALID，并禁止进入 mean_q_top。

六、修复统计量
1. sampled q_top 必须把 basis characters 和随机 non-basis characters
   按其总体数量正确加权。
2. 使用 block/independent-chain cross products 去除 m_hat^2 的有限样本正偏。
3. 将当前 w0 重命名为 w_truth。
4. full weights 时新增 p_succ=max(weights_absolute)。
5. sampled 模式输出由 q_top 推出的 p_succ lower/upper bounds。

七、增加决定性测试
建立一个很小的 CSS code，枚举 sigma_prep、delta_meas、epsilon_data。

令：
    s_prep = sigma_prep XOR delta_meas
    F_total = c_prep(sigma_prep) XOR epsilon_data
    sigma_final = H F_total
    y_eff = s_prep XOR sigma_final

验证对每个求和变量 a，令 e=a XOR F_total 后：

    q(Ha XOR s_prep) p(a XOR F_total)
      ==
    p(e) q(He XOR y_eff).

进一步验证：
1. partition function 完全相等；
2. 每个 logical-sector weight 完全相等；
3. q_top 完全相等；
4. p_succ 完全相等；
5. true-posterior energy 不直接依赖 epsilon_data_true；
6. current legacy_delta_only 在非平凡 H epsilon_data 下与 true 模式不同；
7. q=0 时 true 是 quenched coset，legacy 是 clean；
8. sampled q_top 在人工 character 表上无偏；
9. w_truth 与 p_succ 的反例被锁死；
10. k>10 pairwise q_top 被硬拒绝。

完成后在 conda 环境 12 中运行全部 exp101 tests，
保存 pytest 输出和新增 exact-enumeration 证据。
不要为了让测试通过而放宽任何既有收敛 gate。