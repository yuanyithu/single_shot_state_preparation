# exp102 status

**IMPLEMENTATION / PRE-PILOT — production not started**

The independent registry, bit-identical reference/Numba hard-coset q=0 PT, net-transport
diagnostics, task identity/resume, fail-closed aggregation, publication loader, and pilot cell
runner are implemented. Pilot tuning, held-out certification, remote smoke, and the 6144
production tasks remain to be run. Therefore no threshold curve or scientific result is claimed.

Production requires `engine=numba`; the reference engine is an oracle only. A local nonzero-
syndrome comparison is bit-identical and the combined exp102/exp101 regression is 83/83 PASS.
Cross-node preflight remains required before tuning evidence can be accepted.
