# BP-systematic independence-MH v1 red-team review

This v1 review inherits the mathematical target, proposal, legal P/U/L starts,
fixed 256/2048 clock, full-label `D2_norm` gate, and authority boundary from
`BP_IMH_REVIEW.md`.  V0 produced no report and is permanently excluded because
its receipt path mixed a relative CLI path with an absolute root after writing
one raw.

V1 changes no scientific parameter, method, gate, or resource.  It uses the
fresh contract `exp102.q0_bp_imh.local.v1`, config version v1, and seed namespace
`exp102.q0_bp_imh.local.v1.20260724`.  Before raw generation, a focused test must
prove that both relative and absolute forms of the frozen output directory
resolve to the same canonical path.  V0 manifest, seed identities, and raw are
not reused.
