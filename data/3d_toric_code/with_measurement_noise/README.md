# 3D Toric Code With Measurement Noise（冻结档案）

本目录是只读历史证据源，不是活动实验工作区。当前解释和作图权限见 [3D 最终目录](../../3D_final/README.md) 与 [实验取舍索引](../../3D_final/EXPERIMENT_TRIAGE.md)。目录内旧 README 的 threshold、phase-boundary 或 “论文级” 表述不再自动有效。

- exp34 固定的是 `p=0.0500`（不是 `p=0.005`），扫描 `q=0..0.08`；网格只完成 L=3/4/5 全 q 与 L=6 到 q=0.06，L=7 未运行，不能据此宣称可靠共同 crossing。
- exp35 的 frozen/mixing gate 漏判、exp36 的 sector handling、exp37 旧 AIS/FEP、exp39 的 `Delta f` headline 与 exp40 的低统计相边界均已降级；具体边界见取舍索引。
- exp41/003–006 是 `legacy_delta_only` 的最终模型特定证据；只允许按统一重算的 sign-aware `w0` 展示阈值，`q_top` 仅作伴随曲线。
- 本目录没有当前 `true_posterior` / reduced-MLD 的论文阈值数据。
